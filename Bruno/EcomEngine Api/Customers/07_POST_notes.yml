info:
  name: POST /api/v1/customers/:id/notes
  type: http
  seq: 7

http:
  method: POST
  url: {{base_url}}/api/v1/customers/:id/notes
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "content": "",
        "created_by": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
