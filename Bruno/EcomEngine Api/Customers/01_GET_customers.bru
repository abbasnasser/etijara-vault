meta {
  name: GET /api/v1/customers
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/customers
  body: 
  auth: bearer
}

params:query {
  ~limit: 
  ~page: 
  ~search: 
  ~type: 
}

auth:bearer {
  token: {{auth_token}}
}

