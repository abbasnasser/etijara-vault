info:
  name: GET /api/v1/customers/:id/notes
  type: http
  seq: 6

http:
  method: GET
  url: {{base_url}}/api/v1/customers/:id/notes
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
