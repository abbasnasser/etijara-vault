info:
  name: POST /api/v1/customers/:id/tags
  type: http
  seq: 8

http:
  method: POST
  url: {{base_url}}/api/v1/customers/:id/tags
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "tag": "",
        "color": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
