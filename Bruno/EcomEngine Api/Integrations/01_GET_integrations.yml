info:
  name: GET /api/v1/integrations
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/api/v1/integrations
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
