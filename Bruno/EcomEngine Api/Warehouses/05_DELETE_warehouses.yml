info:
  name: DELETE /api/v1/warehouses/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: {{base_url}}/api/v1/warehouses/:id
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
