meta {
  name: POST /api/v1/warehouses/:id/default
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/v1/warehouses/:id/default
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
