info:
  name: PATCH /api/v1/warehouses/:id/toggle
  type: http
  seq: 7

http:
  method: PATCH
  url: {{base_url}}/api/v1/warehouses/:id/toggle
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
