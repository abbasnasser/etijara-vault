meta {
  name: POST /api/v1/warehouses/inventory/alerts/:alertId/resolve
  type: http
  seq: 15
}

post {
  url: {{base_url}}/api/v1/warehouses/inventory/alerts/:alertId/resolve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
