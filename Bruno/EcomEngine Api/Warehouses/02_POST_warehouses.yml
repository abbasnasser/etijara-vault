info:
  name: POST /api/v1/warehouses
  type: http
  seq: 2

http:
  method: POST
  url: {{base_url}}/api/v1/warehouses
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
