info:
  name: GET /api/v1/warehouses/:id/inventory
  type: http
  seq: 8

http:
  method: GET
  url: {{base_url}}/api/v1/warehouses/:id/inventory
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
