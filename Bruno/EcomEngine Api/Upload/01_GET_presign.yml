info:
  name: GET /api/v1/upload/presign
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/api/v1/upload/presign
  params:
    query:
      - name: filename
        value: ""
        enabled: false
      - name: content_type
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
