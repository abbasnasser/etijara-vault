info:
  name: GET /api/email/auth
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/api/email/auth
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
