meta {
  name: GET /api/email/auth/callback
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/email/auth/callback
  body: 
  auth: bearer
}

params:query {
  ~code: 
}

auth:bearer {
  token: {{auth_token}}
}

