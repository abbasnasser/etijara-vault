info:
  name: GET /api/email/auth/callback
  type: http
  seq: 2

http:
  method: GET
  url: {{base_url}}/api/email/auth/callback
  params:
    query:
      - name: code
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
