info:
  name: GET /api/email/setup
  type: http
  seq: 3

http:
  method: GET
  url: {{base_url}}/api/email/setup
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
