info:
  name: GET /health
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/health
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
