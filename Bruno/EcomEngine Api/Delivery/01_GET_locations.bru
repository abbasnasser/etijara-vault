meta {
  name: GET /api/v1/delivery/:provider/locations
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/delivery/:provider/locations
  body: 
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

