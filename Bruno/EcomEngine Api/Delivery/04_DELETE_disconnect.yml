info:
  name: DELETE /api/v1/integrations/:provider/disconnect
  type: http
  seq: 4

http:
  method: DELETE
  url: {{base_url}}/api/v1/integrations/:provider/disconnect
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
