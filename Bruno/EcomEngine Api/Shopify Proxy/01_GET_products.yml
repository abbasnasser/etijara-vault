info:
  name: GET /api/shopify-proxy/products
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/api/shopify-proxy/products
  params:
    query:
      - name: workspace_id
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
