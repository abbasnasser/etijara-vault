info:
  name: GET /api/shopify-proxy/stock
  type: http
  seq: 2

http:
  method: GET
  url: {{base_url}}/api/shopify-proxy/stock
  params:
    query:
      - name: workspace_id
        value: ""
        enabled: false
      - name: sku
        value: ""
        enabled: false
      - name: variantId
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
