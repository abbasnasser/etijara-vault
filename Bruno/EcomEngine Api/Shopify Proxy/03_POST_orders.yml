info:
  name: POST /api/shopify-proxy/orders
  type: http
  seq: 3

http:
  method: POST
  url: {{base_url}}/api/shopify-proxy/orders
  params:
    query:
      - name: workspace_id
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "customerEmail": "",
        "customerName": "",
        "currency": "SAR",
        "items": [{
          "variantId": "",
          "sku": "",
          "title": "Sample Title",
          "quantity": 1,
          "price": "29.99"
        }]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
