info:
  name: OPTIONS /api/shopify-proxy/orders
  type: http
  seq: 4

http:
  method: OPTIONS
  url: {{base_url}}/api/shopify-proxy/orders
  params:
    query:
      - name: workspace_id
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
