info:
  name: GET /api/v1/products/:id/variants
  type: http
  seq: 11

http:
  method: GET
  url: {{base_url}}/api/v1/products/:id/variants
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
