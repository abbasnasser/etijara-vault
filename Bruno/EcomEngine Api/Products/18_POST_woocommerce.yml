info:
  name: POST /api/v1/products/:id/woocommerce
  type: http
  seq: 18

http:
  method: POST
  url: {{base_url}}/api/v1/products/:id/woocommerce
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
