info:
  name: GET /api/v1/products/:id/images
  type: http
  seq: 7

http:
  method: GET
  url: {{base_url}}/api/v1/products/:id/images
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
