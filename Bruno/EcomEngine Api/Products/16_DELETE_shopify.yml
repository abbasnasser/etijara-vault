info:
  name: DELETE /api/v1/products/:id/shopify
  type: http
  seq: 16

http:
  method: DELETE
  url: {{base_url}}/api/v1/products/:id/shopify
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
