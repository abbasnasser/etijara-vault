info:
  name: PUT /api/v1/products/:id/images/:imageId
  type: http
  seq: 9

http:
  method: PUT
  url: {{base_url}}/api/v1/products/:id/images/:imageId
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "image": {
          "alt": null,
          "position": null
        },
        "altText": null,
        "position": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
