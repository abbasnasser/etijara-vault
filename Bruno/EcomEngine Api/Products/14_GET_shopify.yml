info:
  name: GET /api/v1/products/:id/shopify
  type: http
  seq: 14

http:
  method: GET
  url: {{base_url}}/api/v1/products/:id/shopify
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
