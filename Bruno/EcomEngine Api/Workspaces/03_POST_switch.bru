meta {
  name: POST /api/v1/workspaces/:id/switch
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/workspaces/:id/switch
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
