info:
  name: GET /api/v1/subscription
  type: http
  seq: 4

http:
  method: GET
  url: {{base_url}}/api/v1/subscription
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
