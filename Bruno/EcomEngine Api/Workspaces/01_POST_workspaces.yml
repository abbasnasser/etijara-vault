info:
  name: POST /api/v1/workspaces
  type: http
  seq: 1

http:
  method: POST
  url: {{base_url}}/api/v1/workspaces
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "name": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
