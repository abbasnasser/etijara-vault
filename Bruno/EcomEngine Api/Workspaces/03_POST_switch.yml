info:
  name: POST /api/v1/workspaces/:id/switch
  type: http
  seq: 3

http:
  method: POST
  url: {{base_url}}/api/v1/workspaces/:id/switch
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
