info:
  name: GET /api/wc/return
  type: http
  seq: 2

http:
  method: GET
  url: {{base_url}}/api/wc/return
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
