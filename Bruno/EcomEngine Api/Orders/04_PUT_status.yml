info:
  name: PUT /api/v1/orders/:id/status
  type: http
  seq: 4

http:
  method: PUT
  url: {{base_url}}/api/v1/orders/:id/status
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "status": "active"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
