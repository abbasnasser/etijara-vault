meta {
  name: POST /api/v1/orders/:id/cancel
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/v1/orders/:id/cancel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
