info:
  name: PUT /api/v1/orders/:id/ship
  type: http
  seq: 5

http:
  method: PUT
  url: {{base_url}}/api/v1/orders/:id/ship
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "tracking_number": "",
        "carrier": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
