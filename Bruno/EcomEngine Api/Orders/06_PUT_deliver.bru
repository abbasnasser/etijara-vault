meta {
  name: PUT /api/v1/orders/:id/deliver
  type: http
  seq: 6
}

put {
  url: {{base_url}}/api/v1/orders/:id/deliver
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
