info:
  name: PUT /api/v1/orders/:id/deliver
  type: http
  seq: 6

http:
  method: PUT
  url: {{base_url}}/api/v1/orders/:id/deliver
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
