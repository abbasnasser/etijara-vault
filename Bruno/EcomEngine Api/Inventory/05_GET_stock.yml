info:
  name: GET /api/v1/stock
  type: http
  seq: 5

http:
  method: GET
  url: {{base_url}}/api/v1/stock
  params:
    query:
      - name: sku
        value: ""
        enabled: false
      - name: variantId
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
