info:
  name: GET /api/v1/inventory/:variantId/history
  type: http
  seq: 4

http:
  method: GET
  url: {{base_url}}/api/v1/inventory/:variantId/history
  params:
    query:
      - name: limit
        value: ""
        enabled: false
      - name: offset
        value: ""
        enabled: false
  auth:
    type: bearer
    token: {{auth_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
