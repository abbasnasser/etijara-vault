meta {
  name: POST /api/webhooks/shopify/inventory
  type: http
  seq: 12
}

post {
  url: {{base_url}}/api/webhooks/shopify/inventory
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
