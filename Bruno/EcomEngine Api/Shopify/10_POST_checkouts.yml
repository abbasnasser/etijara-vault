info:
  name: POST /api/webhooks/shopify/checkouts
  type: http
  seq: 10

http:
  method: POST
  url: {{base_url}}/api/webhooks/shopify/checkouts
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
