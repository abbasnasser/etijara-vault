meta {
  name: GET /api/auth/shopify/callback
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/auth/shopify/callback
  body: 
  auth: bearer
}

params:query {
  ~code: 
  ~shop: 
  ~state: 
  ~hmac: 
  ~timestamp: 
}

auth:bearer {
  token: {{auth_token}}
}

