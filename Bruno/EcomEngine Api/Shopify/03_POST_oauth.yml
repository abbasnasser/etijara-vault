info:
  name: POST /api/v1/integrations/shopify/oauth
  type: http
  seq: 3

http:
  method: POST
  url: {{base_url}}/api/v1/integrations/shopify/oauth
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {
        "shopName": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
