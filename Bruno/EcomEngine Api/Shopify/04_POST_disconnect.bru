meta {
  name: POST /api/v1/integrations/shopify/disconnect
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/integrations/shopify/disconnect
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
