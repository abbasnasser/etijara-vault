info:
  name: POST /api/webhooks/shopify/orders
  type: http
  seq: 9

http:
  method: POST
  url: {{base_url}}/api/webhooks/shopify/orders
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
