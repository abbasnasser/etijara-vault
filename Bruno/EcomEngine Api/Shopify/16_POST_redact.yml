info:
  name: POST /api/webhooks/shopify/compliance/shop/redact
  type: http
  seq: 16

http:
  method: POST
  url: {{base_url}}/api/webhooks/shopify/compliance/shop/redact
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
