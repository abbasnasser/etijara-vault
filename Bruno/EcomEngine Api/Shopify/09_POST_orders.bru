meta {
  name: POST /api/webhooks/shopify/orders
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/webhooks/shopify/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
