info:
  name: POST /api/webhooks/shopify/capture
  type: http
  seq: 13

http:
  method: POST
  url: {{base_url}}/api/webhooks/shopify/capture
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
