meta {
  name: POST /api/webhooks/shopify/compliance/customers/redact
  type: http
  seq: 15
}

post {
  url: {{base_url}}/api/webhooks/shopify/compliance/customers/redact
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
