meta {
  name: POST /api/webhooks/shopify
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/webhooks/shopify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
{}
}
