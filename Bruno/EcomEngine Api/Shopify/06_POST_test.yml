info:
  name: POST /api/v1/integrations/shopify/test
  type: http
  seq: 6

http:
  method: POST
  url: {{base_url}}/api/v1/integrations/shopify/test
  auth:
    type: bearer
    token: {{auth_token}}
  body:
    mode: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
